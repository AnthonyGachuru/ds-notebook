# Code Snippets

## Albon

http://chrisalbon.com/

## Sampling File

```{bash, eval = F}
pip install subsample
gzcat ginormous.csv.gz | subsample -n 100000 > sampled.csv.gz
```

## Hold

* ggplot(df, aes(fitted_probability, binary_class_indicator)) +
geom_smooth(): a quick way of visualizing model calibration without having to manually bin any observations

* Rule of thumb for bins in histogram: `ggplot() + geom_histogram(aes(x), binwidth = diff(range(x)) / (2 * IQR(x) / length(x)^(1/3)))`.

* [Optimal Bins For Histogram](https://stats.stackexchange.com/questions/798/calculating-optimal-number-of-bins-in-a-histogram/862#862)

http://blog.rtwilson.com/how-to-rescue-lost-code-from-a-jupyteripython-notebook/
